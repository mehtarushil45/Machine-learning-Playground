{
  "name": "ml-playground",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "pnpm --filter web dev",
    "build": "pnpm --filter web build && node -e \"fs.cpSync('apps/web/dist', 'dist', { recursive: true })\"",
    "lint": "pnpm --filter web lint",
    "preview": "pnpm --filter web preview"
  },
  "dependencies": {
    "typescript": "5.9.3"
  }
}
