[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "ml-playground-worker"
version = "0.1.0"
description = "ML Playground Worker Service"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
where = ["."]
include = ["core*", "tasks*", "celery_app*", "worker*"]
